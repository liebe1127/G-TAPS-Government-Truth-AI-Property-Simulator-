# G-TAPS Environment
# Copy to .env locally if needed. Prefer Cursor Cloud Secrets in cloud agents.

# Example placeholders — do not put real credentials here.
# OPENAI_API_KEY=
# PUBLIC_DATA_API_KEY=
